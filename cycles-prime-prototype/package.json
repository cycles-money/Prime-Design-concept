{
  "name": "cycles-prime-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && touch .nojekyll && rm -rf .git && git init -q -b gh-pages && git add -A && git -c user.email=deploy@cycles.money -c user.name=Deploy commit -q -m Deploy && git push -q --force https://github.com/cycles-money/Prime-Design-concept.git gh-pages && rm -rf .git && echo '✓ deployed to https://cautious-adventure-y7477yo.pages.github.io/'"
  },
  "dependencies": {
    "@number-flow/react": "^0.6.0",
    "agentation": "^2.3.3",
    "cryptocurrency-icons": "^0.18.1",
    "lucide-react": "^0.577.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.0",
    "torph": "^0.0.9"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
